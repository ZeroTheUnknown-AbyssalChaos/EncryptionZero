/**
 *  Custom Made Encryption & Decryption Method
 * <p>
 * =#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=
 * <p>
 * {@link EncryptionZero.Test} ist das Interface in dem Frei Code veränderbar und nutzt bar ist,
 *      wie auch Optionen und ein Beispiel besitzt
 * <p>
 * {@link EncryptionZero.text.TextFormat} ist eine Konsolen und String Text hilfe, die Farben änderung,
 *      wie auch Text art änderung zuläst, wie auch hilfsoptionen bietet. zum Beispiel:
 *      <P>
 *          {@link EncryptionZero.text.TextFormat#line()} die eine Linie als String zurück gibt,
 *              die 500 zeichen lang aus '-' besteht <p>
 *          {@link EncryptionZero.text.TextFormat#line(char)} die eine linie als String zurück gibt,
 *              die 500 zeichen lang aus gewünschtem char besteht <p>
 *          {@link EncryptionZero.text.TextFormat#line(int)} die eine linie als String zurück gibt,
 *              die beliebig lang aus '-' besteht <p>
 *          {@link EncryptionZero.text.TextFormat#line(int, char)} die eine als String zurück gibt,
 *              die beliebig lang aus gewünschtem char besteht <p>
 *          <P>
 *          {@link EncryptionZero.text.TextFormat#printLine()} gibt {@code TextFormat.line()}
 *              direkt in die Konsole aus<p>
 *          {@link EncryptionZero.text.TextFormat#printLine(char)} gibt {@code TextFormat.line(char)}
 *              direkt in die Konsole aus<p>
 *          {@link EncryptionZero.text.TextFormat#printLine(int)} gibt {@code TextFormat.line(int)}
 *              direkt in die Konsole aus<p>
 *          {@link EncryptionZero.text.TextFormat#printLine(int char)} gibt {@code TextFormat.line(int char)}
 *              direkt in die Konsole aus<p>
 *
 * =#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=#=
 *  <p>
 *  <p>
 * @apiNote
 * Author:&#0009;&#0009;Zero aka. stud106272 aka. Jeremi Th.G.
 * <p> Version:&#0009;&#0009;1.0.1
 * <p> Version-Date: &#0009;14.06.2024
 */
package EncryptionZero;
